# WireMock fixtures

This directory is intentionally empty at the platform-baseline stage. Later work may add deterministic mappings and response bodies; keep credentials and live endpoints out of fixtures.
